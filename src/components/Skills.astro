---
import SmallCard from "./smallCard/SmallCard.astro";
---

<section>
    <h2>How can I help?</h2>
    <div class="skills__container">
        <SmallCard
            src="/assets/abilities__square.svg"
            alt="square icon with nodes that represents a website being built"
            skill="Websites from scratch"
            description="With firebase as backend data base,full flow from searching to checkout."
        />
        <SmallCard
            src="/assets/abilities__code.svg"
            alt="code icon that represents the technologies I work with"
            skill="Technologies"
            description="The technologies I use the most are Astro and Next.js, with TypeScript, to build the frontend and TailwindCSS to style."
        />
        <SmallCard
            src="/assets/abilities__square.svg"
            alt="square icon with nodes that represents a website being built"
            skill="Websites from scratch"
            description="With firebase as backend data base,full flow from searching to checkout."
        />
    </div>
</section>

<style>
    section {
        display: flex;
        flex-direction: column;
        gap: 2rem;

        margin-block: 3rem;
    }

    h2 {
        font-size: var(--fs__20);
        font-weight: var(--fw__medium);
        text-align: center;
    }

    .skills__container {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    @media (min-width: 768px) {

        h2 {
            font-size: var(--fs__28);
        }

        .skills__container {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;

            overflow-x: scroll;
            scroll-snap-type: x mandatory;

            scrollbar-width: none;
        }

        .skills__container::-webkit-scrollbar {
            display: none;
        }
    }

    @media (min-width: 768px) {

        h2 {
            font-size: var(--fs__40);
        }
    }
</style>
